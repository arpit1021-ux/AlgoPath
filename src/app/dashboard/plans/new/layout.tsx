export const metadata = {
  title: "Create a Plan",
  description: "Build a personalized LeetCode roadmap from your timeline and target companies.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
