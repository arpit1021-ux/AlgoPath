export const metadata = {
  title: "Analytics",
  description: "Interview readiness, weak topics and pace for this plan.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
