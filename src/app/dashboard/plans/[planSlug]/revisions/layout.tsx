export const metadata = {
  title: "Revisions",
  description: "Spaced-repetition reviews due for this plan.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
