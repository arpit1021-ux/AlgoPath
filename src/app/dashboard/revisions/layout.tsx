export const metadata = {
  title: "Revisions",
  description: "Spaced-repetition reviews due across all your plans.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
